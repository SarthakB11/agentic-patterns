"""Runnable examples, one folder per agentic pattern.

Each subpackage has a `main` module runnable from the repo root:

    python -m patterns.<name>.main

Every example runs offline against the scripted mock provider by default.
"""
